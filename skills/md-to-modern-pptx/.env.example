# Preferred env var names (new gateway config)
CHERRY_BASE_URL=https://your-gateway.example.com
CHERRY_API_KEY=your_key_here
CHERRY_MODEL=gemini-3-pro-image-preview
# Optional: how to send API key to the gateway: goog | bearer | both
CHERRY_AUTH_MODE=goog

# Backward-compatible names (older config)
GEMINI_BASE_URL=https://your-gateway.example.com
GEMINI_API_KEY=your_key_here
GEMINI_MODEL=gemini-3-pro-image-preview
# Optional: how to send API key to the gateway: goog | bearer | both
# - goog: only x-goog-api-key (most Gemini proxies)
# - bearer: only Authorization: Bearer
# - both: send both + x-api-key
GEMINI_AUTH_MODE=goog
# If your gateway requires a specific header, this script already sends:
# - Authorization: Bearer <key>
# - X-Api-Key: <key>
# - X-Goog-Api-Key: <key>
